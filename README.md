# README
This tool uses
- uv
- bun

The goal is to generate linear walkthrough explanations of code as either:
- markdown files (for embedding in github comments)
- static html files for embedding distribution
- interactive react page with an embedded pty of claude code for further question / understanding.

This will be shipped as a claude code skill, running via uv (and embedded scripts).
